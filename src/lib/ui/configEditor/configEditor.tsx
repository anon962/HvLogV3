// @ts-ignore

import { App } from "@/lib/app/app"
import "@/lib/ui/global.css"
import { FC } from "react"
import { Card } from "../shadcn/card"

export const ConfigEditor: FC<{ app: App }> = (props: {
    app: App
}) => {
    0
    return <Card></Card>
}
